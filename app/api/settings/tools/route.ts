import {NextRequest,NextResponse} from 'next/server';import {getSession} from '@/lib/session';import {getToolPreferences,setToolPreferences,TOOL_KEYS} from '@/lib/tool-preferences';export const runtime='nodejs';export const dynamic='force-dynamic';
export async function GET(){const session=await getSession();if(!session)return NextResponse.json({error:'Connect Google first.'},{status:401});return NextResponse.json({tools:await getToolPreferences()},{headers:{'Cache-Control':'no-store'}});}
export async function POST(req:NextRequest){const session=await getSession();if(!session)return NextResponse.json({error:'Connect Google first.'},{status:401});try{const body=await req.json();const tools=Object.fromEntries(TOOL_KEYS.map(k=>[k,body?.tools?.[k]!==false]));return NextResponse.json({tools:await setToolPreferences(tools)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Could not save tool settings.'},{status:400});}}