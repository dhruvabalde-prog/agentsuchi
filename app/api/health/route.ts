import {NextResponse} from 'next/server';export const runtime='nodejs';export const dynamic='force-dynamic';export async function GET(){const checks={googleClient:Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET),gemini:Boolean(process.env.GEMINI_API_KEY),session:Boolean(process.env.SESSION_SECRET&&process.env.SESSION_SECRET.length>=32),appUrl:Boolean(process.env.APP_URL)};const ok=Object.values(checks).every(Boolean);return NextResponse.json({ok,checks},{status:ok?200:503,headers:{'Cache-Control':'no-store'}});}