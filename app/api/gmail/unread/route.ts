import {NextResponse} from 'next/server';import {getSession}from '@/lib/session';import {authorizedClient,listUnreadNeedingReply}from '@/lib/google';import {getToolPreferences}from '@/lib/tool-preferences';export const runtime='nodejs';export const dynamic='force-dynamic';export async function GET(){try{const s=await getSession();if(!s?.accessToken)return NextResponse.json({error:'Connect Google first.'},{status:401});if(!(await getToolPreferences()).gmail)return NextResponse.json({error:'Gmail is disabled in Settings.'},{status:403});return NextResponse.json({emails:await listUnreadNeedingReply(authorizedClient(s),10)},{headers:{'Cache-Control':'no-store'}});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed to load unread email.'},{status:500});}}